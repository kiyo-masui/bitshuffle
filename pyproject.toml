# Include dependencies when building wheels on cibuildwheel
[build-system]
requires = [
    "setuptools>=0.7",
    "Cython>=0.19",
    "numpy>=1.6.1",
    "h5py==3.1.0",
]

build-backend = "setuptools.build_meta"
